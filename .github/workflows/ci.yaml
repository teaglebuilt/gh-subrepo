name: CI

on:
  pull_request:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24.1'
    - name: Run tests
      run: |
        go test ./...
